import { StyleSheet } from "react-native";
import Colors from "../../utils/Colors";

const starshipListStyle = StyleSheet.create({
    container: {
        flex: 1,
        marginHorizontal: 20,
    },
    list: {
        padding: 10,
    },

});

export default starshipListStyle;